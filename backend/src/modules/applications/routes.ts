// import { Router } from 'express';
// import {
//   createApplication,
//   getApplication,
//   markStepComplete
// } from './controller';

// const router = Router();

// router.post('/', createApplication);            // Create application
// router.get('/:userId', getApplication);         // View user's app + progress
// router.patch('/step/:stepId', markStepComplete); // Mark step complete

// export default router;
